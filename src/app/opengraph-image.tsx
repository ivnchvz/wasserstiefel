import { ogCard, OG_SIZE } from "@/lib/ogCard";

export const alt = "wasserstiefel — an insight";
export const size = OG_SIZE;
export const contentType = "image/png";

export default function Image() {
  return ogCard();
}
